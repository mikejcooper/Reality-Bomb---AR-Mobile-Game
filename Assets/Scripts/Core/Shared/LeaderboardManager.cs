using System;
using UnityEngine;

public class LeaderboardManager : MonoBehaviour {

	public void Start () {
//		Debug.LogError ("starting Leaderboadr manager");
//		foreach (var player in PlayerDataManager.Instance.PlayerDataList) {
//			Debug.LogError(string.Format("player id: {0}, name: {1}, finalPosition: {2}, remainingTime: {3}", player.ServerId, player.Name, player.FinishPosition, player.RemainingTime));
//		}
	}

}


